# Base URL of the Django REST API (no trailing slash)nhn
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000/api

# For a deployed frontend, use the public HTTPS Django API URL instead.
# Example:
# NEXT_PUBLIC_API_URL=https://your-api.example.com/api
